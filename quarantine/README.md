# quarantine

The previous tree, mothballed. Not the product. Do not add features
here. Do not `cargo add` this crate as a dependency.

Fault in by copying the smallest piece that earns its place, then
rewriting it to the five words in `docs/kernel.md`. If a file needs a
funny name to make sense, leave it here.
